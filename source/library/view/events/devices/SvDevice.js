"use strict";

/**
 * @module library.view.events.devices
 */

/**
 * @class SvDevice
 * @extends ProtoClass
 * @classdesc Represents a device.
 */
(class SvDevice extends ProtoClass {

    /**
     * @description Initializes prototype slots for the SvDevice class.
     * @private
     * @category Initialization
     */
    initPrototypeSlots () {
    }

    /**
     * @description Initializes the SvDevice instance.
     * @returns {SvDevice} The initialized SvDevice instance.
     * @category Initialization
     */
    init () {
        super.init();
        return this;
    }

}.initThisClass());
